name: Ping Bot

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Call /ping endpoint
        run: curl -s https://hmod-dbl8.onrender.com/ping
# refresh schedule
